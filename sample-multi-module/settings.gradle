rootProject.name = 'static-analysis-sample-multi'
include ':app'
include ':core'

includeBuild('../') {
    dependencySubstitution {
        substitute module('com.gradleup:static-analysis-plugin') with project(':plugin')
    }
}
