rootProject.name = 'static-analysis-sample'
include ':app'

includeBuild('../') {
    dependencySubstitution {
        substitute module('com.gradleup:gradle-static-analysis-plugin') with project(':plugin')
    }
}
